<launch>
  <node name="waypoints_navigation_node"
        pkg="tiago_search_detect"
        type="waypoints_navigation_node"
        output="screen">
    <!-- This parameter is now retrieved via the private node handle -->
    <param name="waypoints_file" value="$(find tiago_search_detect)/config/tiago_waypoints.yaml" />
  </node>

  <node name="rviz" pkg="rviz" type="rviz" output="screen"/>
</launch>
